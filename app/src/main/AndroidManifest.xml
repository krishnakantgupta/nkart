<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kk.nkart">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.view.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.view.authentication.login.LoginActivity"/>
        <activity android:name=".ui.view.authentication.registeration.RegistrationActivity"/>
        <activity android:name=".ui.view.CartActivity" />
        <activity android:name=".ui.view.category.CategoryActivity" />
        <activity android:name=".ui.view.plp.ProductListActivity" />
        <activity android:name=".ui.view.pdp.ProductDetailsActivity" />
        <activity android:name=".ui.view.dashboard.DashBoardActivity" />

    </application>

</manifest>